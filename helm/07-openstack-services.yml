---
- name: OpenStack 부가 서비스 배포 (Octavia LB + Designate DNS)
  hosts: helm1
  gather_facts: false
  become: true
  vars:
    openstack_release: "2025.1"
    features: "admin_creds 2025.1 ubuntu_noble tls"
    overrides_dir: /root/overrides
    overrides_url: https://opendev.org/openstack/openstack-helm/raw/branch/master/values_overrides
    admin_username: coremax
    admin_password: "coremax1@#"
  environment:
    OPENSTACK_RELEASE: "{{ openstack_release }}"
    FEATURES: "{{ features }}"
    OVERRIDES_DIR: "{{ overrides_dir }}"
    OVERRIDES_URL: "{{ overrides_url }}"
  tasks:
    # ==========================================================
    # 사전 정리
    # ==========================================================
    - name: Pending 상태 Helm release 정리
      shell: |
        STATUS=$(helm status {{ item }} -n openstack -o json 2>/dev/null | jq -r '.info.status // "not_installed"')
        if [[ "$STATUS" == pending-* ]]; then
          echo "CLEANING: {{ item }} (status: $STATUS)"
          helm uninstall {{ item }} -n openstack --no-hooks 2>/dev/null || true
        fi
      loop:
        - redis
        - powerdns
        - octavia
        - designate
      register: pending_cleanup
      changed_when: "'CLEANING' in pending_cleanup.stdout"

    - name: OVN DB 서비스 확인 (Octavia driver-agent 연결용)
      shell: kubectl get svc -n openstack -o name | grep -i ovn
      register: ovn_services
      changed_when: false

    - name: OVN 서비스 목록 출력
      debug:
        var: ovn_services.stdout_lines

    # ==========================================================
    # 오버라이드 다운로드 + 준비
    # ==========================================================
    - name: 서비스 차트 값 오버라이드 다운로드
      shell: "helm osh get-values-overrides -d -u {{ overrides_url }} -p {{ overrides_dir }} -c {{ item }} {{ features }}"
      loop:
        - octavia
        - designate
      changed_when: false

    - name: 서비스별 오버라이드 디렉토리 생성
      file:
        path: "{{ overrides_dir }}/{{ item }}"
        state: directory
        mode: "0755"
      loop:
        - octavia
        - designate

    - name: Octavia admin 크레덴셜 오버라이드 생성
      copy:
        dest: "{{ overrides_dir }}/octavia/admin_creds.yaml"
        content: |
          endpoints:
            identity:
              auth:
                admin:
                  username: {{ admin_username }}
                  password: "{{ admin_password }}"

    - name: Designate admin 크레덴셜 오버라이드 생성
      copy:
        dest: "{{ overrides_dir }}/designate/admin_creds.yaml"
        content: |
          endpoints:
            identity:
              auth:
                admin:
                  username: {{ admin_username }}
                  password: "{{ admin_password }}"

    # ----------------------------------------------------------
    # Octavia OVN 모드 오버라이드 (amphora 비활성화)
    # ----------------------------------------------------------
    - name: Octavia OVN 모드 오버라이드 생성
      copy:
        dest: "{{ overrides_dir }}/octavia/octavia_ovn.yaml"
        content: |
          manifests:
            daemonset_health_manager: false
            daemonset_worker: false
            deployment_housekeeping: false
            secret_credential_keys: false
          conf:
            octavia:
              api_settings:
                enabled_provider_drivers: "ovn:OVN provider"
                default_provider_driver: ovn
              controller_worker:
                network_driver: allowed_address_pairs_driver
                compute_driver: compute_nova_driver
                amphora_driver: amphora_noop_driver
              ovn:
                ovn_nb_connection: "tcp:ovn-ovsdb-nb.openstack.svc.cluster.local:6641"
                ovn_sb_connection: "tcp:ovn-ovsdb-sb.openstack.svc.cluster.local:6642"
          endpoints:
            valkey:
              hosts:
                default: redis

    # ==========================================================
    # Wave 1: Redis + PowerDNS (인프라 서비스, 독립)
    # ==========================================================
    - name: "[Wave 1] Redis 배포 (Octavia TaskFlow jobboard)"
      shell: >
        helm upgrade --install redis openstack-helm/redis
        --namespace=openstack
      changed_when: true

    - name: "[Wave 1] PowerDNS 배포 (Designate 백엔드)"
      shell: >
        helm upgrade --install powerdns openstack-helm/powerdns
        --namespace=openstack
      changed_when: true

    - name: "[Wave 1] 파드 대기 (Redis + PowerDNS)"
      shell: helm osh wait-for-pods openstack
      retries: 30
      delay: 10
      register: result
      until: result.rc == 0
      changed_when: false

    # ==========================================================
    # Wave 2: Octavia + Designate (Keystone 필요, Wave 1 완료 후)
    # ==========================================================
    - name: "[Wave 2] Octavia 배포 (OVN provider mode)"
      shell: >
        helm upgrade --install octavia openstack-helm/octavia
        --namespace=openstack
        $(helm osh get-values-overrides -p {{ overrides_dir }} -c octavia octavia_ovn {{ features }})
      changed_when: true

    - name: "[Wave 2] Designate 배포 (PowerDNS 백엔드)"
      shell: >
        helm upgrade --install designate openstack-helm/designate
        --namespace=openstack
        $(helm osh get-values-overrides -p {{ overrides_dir }} -c designate {{ features }})
      changed_when: true

    - name: "[Wave 2] 파드 대기 (Octavia + Designate)"
      shell: helm osh wait-for-pods openstack
      retries: 60
      delay: 15
      register: result
      until: result.rc == 0
      changed_when: false

    # ==========================================================
    # Wave 3: Neutron 업그레이드 (Designate DNS 드라이버 추가)
    # ==========================================================
    - name: Neutron Designate 통합 오버라이드 생성
      copy:
        dest: "{{ overrides_dir }}/neutron/neutron_designate.yaml"
        content: |
          conf:
            neutron:
              DEFAULT:
                external_dns_driver: designate

    - name: "[Wave 3] Neutron 업그레이드 (Designate DNS 드라이버 활성화)"
      shell: >
        helm upgrade neutron openstack-helm/neutron
        --namespace=openstack
        $(helm osh get-values-overrides -p {{ overrides_dir }} -c neutron neutron_simple neutron_designate {{ features }})
      changed_when: true

    - name: "[Wave 3] 전체 파드 대기"
      shell: helm osh wait-for-pods openstack
      retries: 60
      delay: 15
      register: result
      until: result.rc == 0
      changed_when: false
